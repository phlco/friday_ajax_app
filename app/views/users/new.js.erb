// render the form partial.
// pass it a j to escape javascript and render the
// html correctly
$('#form').html("<%=j render 'form' %>").show();
// hide the list of users while we show the form
$('#users').hide();
